{
    "name": "service-finder",
    "version": "1.0.0",
    "scripts": {
        "start:all": "concurrently \"npm run dev --prefix services-finder-backend\" \"npm run dev --prefix services-finder-frontend\""
    },
    "dependencies": {
        "concurrently": "^7.0.0"
    }
}
